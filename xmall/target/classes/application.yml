# 配置Tomcat端口
server:
  port: 9998
# 配置Spring Boot 项目名
spring:
  application:
    name: xmall
# 配置Dubbo 信息
  dubbo:
    application:
      name: xmall
    protocol:
      name: dubbo
      port: 20880
    registry:
      address: zookeeper://localhost:2181
    scan: com.zhuxintao.xmall